# Installer venv
pip install virtualenv

# Enable remoteSigned PowerShell-scripts:
- I en admin PowerShell-terminal
set-ExecutionPolicy RemoteSigned
- Tryk Yes

# lav venv
python -m venv .venv

# Aktiver venv
.venv\Scripts\activate.ps1

# Installer alle requirements
pip install -r requirements.txt